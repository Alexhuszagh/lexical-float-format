#include <cassert>

typedef int i32;
typedef double f64;

int main() {{
    {type} actual = {value};
    {type} expected = {expected};
    if (expected != expected) {{
        assert(actual != actual);
    }} else {{
        assert(actual == expected);
    }}
    return 0;
}}
